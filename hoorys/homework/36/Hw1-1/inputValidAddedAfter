/*
 * Note that -- is allowed inside a comment,
 * as well as /*
 * but when a * followed by a / is detected,
 * the comment has ended.
 */
CREATE TABLE Persons (
    PersonID int,
    LastName varchar(255),
    FirstName varchar(255),
    Address varchar(255),
    City varchar(255)
) 

